
services:
  frontend:
    build: .

    ports:
      - "3000:3000"
    networks:
      - my-network

  backend:
    build: .
    ports:
      - "2000:3000"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge
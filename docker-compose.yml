
services:
  react-app:
    build: .

    ports:
      - "3000:3000"
    networks:
      - my-network

  backend:
    build: .
    ports:
      - "5000:5000"
    networks:
      - my-network

networks:
  my-network:
    driver: bridge